console.log(true ? 1 : 2);
console.log(false ? 1 : 2);

// edad de ingreso

function verificarEdad(age) {
    return age >= 18 ? 'puede ingresar': 'no puede ingresar'
}

console.log(verificarEdad(18))

// pasa de grado?

function pasaDeGrado(notas) {
    return  notas >= 10 ? 'pasaste de grado!': 'No pasaste de grado :('
}

console.log(pasaDeGrado(9))

///////////////

